name: Sync SDK (push -> sdk repo)

on:
  push:
    branches: ["main"]

jobs:
  build:
    runs-on: ubuntu-latest
    env:
      WORKFLOW: ${{ secrets.WORKFLOW }}

    steps:
      - name: Checkout server repo
        uses: actions/checkout@v4
        with:
          path: server

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: 8.0.x

      - name: Restore dotnet tools
        working-directory: server
        run: dotnet tool restore

      - name: Restore dependencies
        working-directory: server
        run: dotnet restore

      - name: Build
        working-directory: server
        run: dotnet build --no-restore

      - name: Checkout sdk repo
        uses: actions/checkout@v4
        with:
          repository: daechu5657/ui-framework-sdk
          ref: main
          token: ${{ env.WORKFLOW }}
          path: sdk

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: yarn
          cache-dependency-path: sdk/yarn.lock

      - name: Install sdk dependencies
        working-directory: sdk
        run: |
          corepack enable
          yarn install --frozen-lockfile

      - name: Generate sdk types
        id: sync_sdk
        working-directory: sdk
        run: |
          SRC="../server/UiFrameworkServer/swagger.json"

          if [ ! -f "$SRC" ]; then
            echo "Source file not found: $SRC"
            exit 1
          fi

          npx openapi-typescript "$SRC" --enum -o generated/schema.ts
          yarn extract
          yarn prettier

          if [ -n "$(git status --porcelain generated/schema.ts generated/contract.ts)" ]; then
            echo "changed=true" >> "$GITHUB_OUTPUT"
          else
            echo "changed=false" >> "$GITHUB_OUTPUT"
          fi

      - name: Commit and push if changed
        if: steps.sync_sdk.outputs.changed == 'true'
        working-directory: sdk
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add generated/schema.ts generated/contract.ts
          git commit -m "chore: sync sdk types from ui-framework-server"
          git push origin main
